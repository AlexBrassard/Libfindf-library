#include <stdio.h>
#include <stdlib.h>

#include <findf.h>

int main(void)
{
  findf_param_f *sparam = NULL;
  char **patterns = NULL;
  char **file2find = NULL;
  char **search_roots = NULL;
  size_t numof_file2find = 0;
  size_t numof_search_roots = 1;
  size_t numof_patterns = 1;

  if ((
  
  if((sparam = findf_init_param(
